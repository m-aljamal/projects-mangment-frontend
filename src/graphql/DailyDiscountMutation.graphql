mutation createDiscount(
  $date: DateTime!
  $discount: Float!
  $employeeId: String!
  $notes: String!
) {
  createDailyDiscount(
    dailyDiscount: {
      date: $date
      discount: $discount
      employeeId: $employeeId
      notes: $notes
    }
  ) {
    date
    id
    discount
    notes
  }
}
