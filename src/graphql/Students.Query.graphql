query findStudentsByProject($projectId: String!) {
  findStudentsByProject(projectId: $projectId) {
    division {
      divisionName
      divisionNumber
    }
    fatherName
    firstName
    lastName
    id
    phone
    level {
      id
      levelName
      levelNumber
    }
  }
}
